[project]
name = "transformerimplementation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch == 2.2.0",
    "spacy",
    "torchtext == 0.17.0",
    "numpy == 1.26.4",
    "pip>=25.1.1",
    "portalocker"
]
